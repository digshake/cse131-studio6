import edu.princeton.cs.introcs.StdDraw;

public class Methods {

    /**
     * Computes the geometric sum for the first n terms in the series
     * @param n the number of terms to include in the sum
     * @return the sum of the first n terms of the geometric series (1/2 + 1/4 + 1/8 ...)
     */
    public static double geometricSum(int n) {
        //FIXME compute the geometric sum for the first n terms recursively
        return 0;
    }

    /**
     * This method uses iteration to compute the greatest common divisor
     * for the two input values
     * @param p first operand
     * @param q second operand
     * @return greatest common divisor of p and q
     */
    public static int gcdIterative(int p, int q) {
        //FIXME compute the gcd of p and q using iteration (loops)
        return 0;
    }

    /**
     * This method uses recursion to compute the greatest common divisor
     * for the two input values
     * @param p first operand
     * @param q second operand
     * @return greatest common divisor of p and q
     */
    public static int gcdRecursive(int p, int q) {
        //FIXME compute the gcd of p and q using recursion
        return 0;
    }

    /**
     * This method uses recursion to reverse the given array
     * @param array the array to be reversed
     * @return an array with the same data as the input but it reverse order
     */
    public static int[] reverse(int[] array) {
        //FIXME create a helper method that can recursively reverse the given array
        return new int[0];
    }

    /**
     * Draws sierpinski's triangle using a divide and conquer approach
     * @param level The desired recursive depth
     */
    public static void triangle(int level) {
        //FIXME Construct a helper method to recursively draw sierpinski's triangle
    }

}
